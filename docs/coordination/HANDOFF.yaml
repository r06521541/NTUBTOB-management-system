active_task: TASK-064
status: ready_for_codex
next_actor: codex

base_commit: a88f836fd0f8a1c7cad0af294e63b8e574729512
head_commit: null

updated_by: work
updated_at: 2026-08-07T23:20:00+08:00

note: >
  TASK-063 production Phase A migration was executed once after approved preflight and pre-check. The first post-check
  produced 51 metrics and failed only the append-only function raw prosrc MD5; all other exact gates passed and legacy
  aggregate counts matched. Work reproduced the same false result locally by executing the approved SQL with CRLF.
  TASK-064 is approved to normalize line endings only in the read-only fingerprint and add LF/CRLF regression evidence.
  No migration retry, production mutation, downgrade, deployment or notification is allowed.
