active_task: TASK-065
status: changes_requested
next_actor: codex

base_commit: d33a42a9219f33a2bc191deba910de986fcb027e
head_commit: 9b77bd1ed45a207c591057975456149f57fa4a03

updated_by: work
updated_at: 2026-08-07T22:12:07+08:00

note: >
  Work review found blocking fail-open evidence behavior: status=required integer safety metrics accept nonzero values,
  so orphan, unexpected qualification and other unsafe counts can validate. Codex must use explicit metric schemas and
  zero gates, add Phase A RLS/policy/table boundary inventory, bind the rendered mutation to validated approved inventory
  counts, and reject unexpected/inconsistent audit rows. Preserve the accepted append-only rule: exact rollback is only
  pre-commit; post-commit recovery is forward compensation and must never disable the audit trigger.
