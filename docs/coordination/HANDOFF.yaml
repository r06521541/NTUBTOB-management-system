active_task: TASK-198
status: in_progress
next_actor: main-work
claim_id: task-198-main-20260911
lease_version: 1
actor_id: /root
base_commit: cefbd6375086241f68ea3906e16cf63dc2f0e6c6
updated_by: main-work
updated_at: 2026-09-15
note: >
  CURRENT: Owner explicitly approved this exact public source/fake-tests and
  run/build/service-revision/de-identified smoke evidence payload after disclosure
  of the auto-review rejection. Destination remains public GitHub repository
  r06521541/NTUBTOB-management-system, codex/task-198-google-unavailable;
  resume commit/push, one ready PR, required CI and merge. No credential material.
  Source f3ec67eef9b86c62ed79a79610d3005be477f250 remains ACCEPT with196 local
  tests/analyze/format PASS; five coordination updates travel in this delivery.
  This permission resolves publication only, NOT new-build sidefile disposition,
  real signing/upload or any expanded runtime authority. No new runtime operation.
  Reviewer lease9 completed and handled; no outstanding delegated work.
  ACTIVE: Owner requested continue to next genuine gate. Main implements bounded
  Google503 service_unavailable recovery on codex/task-198-google-unavailable,
  base cefbd6375086241f68ea3906e16cf63dc2f0e6c6. Preserve4 Main-owned docs.
  Advisor /root/native_upload_review lease8 design report received/handled;
  lease9 frozen source ACCEPT received/handled on f3ec67eef9b86c62ed79a79610d3005be477f250,
  independent29PASS; packet/review recorded. Main affected196PASS, analyze/formatPASS.
  No runtime/sign/upload/Secret
  mutation; prior sidefile exception applies only to existing build1 continuation.
  Next one readyPR/requiredCI/merge. Before new build2 require resolved audit or
  new exact Owner sidefile disposition; do not ask Owner to repeat smoke tests.
  CURRENT: Owner reports Google login-to-home and subsequent online close/reopen
  both pass without another Google prompt. Owner-reported session restoration,
  not independent token telemetry, unlimited lifetime or full acceptance.
  Owner also confirms disconnected reopen shows offline read-only mode; not proof
  of exhaustive cached data or write-denial tests. Owner now confirms network
  restoration plus reopen returns normal home without login. Login, persistence,
  offline indication and reconnect/reopen smoke passed; not full acceptance.
  Owner now confirms game detail normal. Completed Owner manual packet in TASK top:
  only game_-112001 /2035-02-01 /台大OB vs 虛構對手甲 /虛構球場 A in staging.
  Match target and note existing non-null own reply before any write; absent target/
  no original reply means STOP. Submit one different reply, reopen/readback, then
  restore original with one submission and reopen/readback (at most2 deliberate
  submissions). Stop on error/uncertain, no retries; preserve audit/time history.
  No other people/data/notifications/production or Main runtime mutation.
  Owner reports all4 save/reopen/restore/reopen steps correct. Bounded attendance
  persistence/restoration now Owner-passed; actual reply values/DB rows not collected.
  Six smoke scenarios are complete, not full IOS-TF-01/public readiness. No repeat
  of this successful mutation packet or immediate additional Owner device action.
  Next Main prioritize Google503 error mapping under review/tests and reconcile
  remaining provider/logout/privacy/device/sidefile gaps; no new source work claimed.
  Other mutations/providers/automatic recovery without reopening remain unverified.
  No credential/reinstall or repeated login; misleading503 text/sidefile remain open.
  Supersedes prior one-Google-login next action below.
  PREVIOUS: Owner reports Supabase Resume complete. Main credential-free /me GET
  2026-09-15T12:44:32.251425Z now401 / BEARER_REQUIRED, valid JSON; DB guard passes.
  Next Owner close/reopen installed App, then one Google login using the same willing
  staging-test account; report home/pending-review/other safe error and stop on failure.
  No reinstall/upload/Secret input or Apple login. Google/session acceptance pending;
  misleading503 text remains unfixed and sidefile disposition unchanged.
  Supersedes incomplete-Resume/readiness-blocker notes below.
  PREVIOUS: Owner completed one Google account selection/authorization; App shows
  contractError. Main read-only bound20:22 Google exchange HTTP503 and staging
  revision mobile-api-staging-task157-47ccfb5f at100%. Readiness log operational /
  none / tcp_ok; one credential-free me GET also503 / database readiness unavailable.
  Not a demonstrated schema mismatch or Google rejection. Valid503 is incorrectly
  mapped to contractError by Google coordinator; no source fix yet.
  Owner initiated Supabase Resume and clarifies it is still in progress. Main's
  credential-free me GET12:41:18Z remained503 during that pending operation;
  not a failed-resume result. Next Owner report Resume completion, then Main checks
  readiness before another device login. No repeated Resume or credential input.
  Main did not perform provider mutation or inspect dashboard state.
  No repeated login, reinstall, signing/upload, deployment or DB/provider mutation.
  Full device acceptance and Apple login remain pending. Current block supersedes
  older one-Google-login instructions below; sidefile disposition unchanged.
  Owner now confirms build present in internal group and TestFlight installation
  reaches iPhone welcome screen. Owner-reported first-launch success, not full acceptance.
  Owner screenshot now shows logged-out page with Apple/Google and icon-only LINE;
  rendering confirmed, repeat cold-start not independently confirmed. Next Owner:
  one Google login using own willing staging-test account in provider UI; report App
  return or safe error, no repeated auth/link attempts. Do not start Apple login.
  No repeated assignment/upload/credential action. Session/API/offline tests pending;
  unresolved sidefile finding and one-time disposition unchanged. Supersedes pending below.
  Latest Owner screenshot: Version1.0.0/build1 Ready to Submit (90days), no longer
  Missing Compliance. Eligible for internal distribution per Apple, not formal review.
  Owner next resumes approved Add Builds1.0.0(1) once to NTUBTOB Owner Internal only,
  then installs via TestFlight and reports launch. No assignment/device success yet.
  No re-upload/other testers/public review; accepted sidefile-risk disposition unchanged.
  This observed state supersedes the questionnaire gates below, not legal approval.
  Owner shows four encryption options; Main technical recommendation option2:
  http1.6.0 Client -> IOClient -> Dart HttpClient/BoringSSL, not Apple OS-only.
  Next select option2 and inspect follow-up, no submission or guessed France answer.
  No legal exemption/plist/binary change; current task top supersedes generic prompt below.
  Latest Owner dialog for NTUBTOB Owner Internal: iOS1.0.0/build1 Missing Compliance,
  Add disabled; assignment/install not completed. Owner next Cancel then TestFlight
  iOS build Manage/Provide Export Compliance Information and show exact questions.
  Do not guess exemptions or submit a declaration; no re-upload or plist/key changes.
  This compliance gate supersedes the manual Add instruction until resolved.
  Owner reports selected existing internal group has only Owner and no builds.
  Owner explicitly answered "好" to one-time continuation with sidefile audit unresolved.
  Next Owner action per TASK top manual packet: Add Builds, existing1.0.0(1), Next,
  enter Owner-only install/launch/basic-page test note, Add once to this group only.
  No actual assignment claimed yet; then Owner installs via TestFlight and reports launch.
  No signing/upload/key action, blanket diagnostic waiver, auto-distribution or other testers.
  Stop on changed group scope, unavailable build or unexpected compliance/terms question.
  Latest manual packet supersedes earlier read-only/disposition gates below.
  Latest Owner screenshot: Build Uploads 1.0.0 (1) green Complete, displayed
  Sep15 2026 5:42PM. Apple defines Complete as successfully processed; this is
  Owner Console evidence, not Main API verification or tester/device evidence.
  Do not re-upload. Next Owner read-only check: existing internal group's tester
  membership and 1.0.0 (1) build listing, no assignment/invitation yet.
  Runner sidefile audit remains unresolved independently of upload success.
  This evidence supersedes receipt/processing pending statements below.
  Run34953146969/job104328690147 completed failure on base_commit/main/attempt1.
  Safe records show SIGNED_COPY_READY, signature and signing cleanup true;
  native upload attempted=true, observed_exit=0, process_stopped=true, failure=null.
  STOP is secondary native_sidefiles/EXTERNAL_METADATA_CHANGED, not native rejection.
  Final upload audit OWNED_PATHS_ABSENT, signing audit ABSENT, artifacts0.
  External Logs/Caches contents/cause remain unknown; do not claim exhaustive absence.
  No retry/re-dispatch/new build number/private input. Apple processing still unverified.
  Browser getState initialization failed missing kernel-assets path twice including reset;
  no live ASC inspection. Owner next: report existing App TestFlight 1.0.0 (1) status
  or screenshot, without upload/group changes. Main then resumes read-only reconciliation.
  This terminal evidence and Owner observation gate supersede older waiting notes below.
  Owner selected upload and dispatched run34953146969 ONCE. Main GET verifies
  exact repo/workflow/main/cefbd6375086241f68ea3906e16cf63dc2f0e6c6/attempt1,
  Owner actor, title1.0.0/build1. Waiting ONLY ios-native-signing Environment21904102114,
  Owner reviewer, timer0. Owner must approve THIS run, not dispatch or rerun again.
  Main has not approved it; no native signing/upload result yet. Latest gate supersedes below.
  PR263 merged cefbd6375086241f68ea3906e16cf63dc2f0e6c6, exact reviewed tree;
  CI34935268030 success16/16 on7d252318592f628d2c97dff6fce90c5ddc8ddbb0.
  Fresh read-only ASC setup ALREADY_PRESENT/protection PASS, no private input/write.
  Owner gate now: manual native workflow dispatch ONCE on main, approved_sha=base_commit,
  version1.0.0/build1, upload_to_testflight=true, then approve ios-native-signing.
  Reuse four Secrets, no P12 password. No concurrent ASC group settings edits.
  Main has NOT signed/uploaded this slice; next read run/result, then ASC processing
  and exact distribution. STOP/unknown means read-only review, not rerun/new dispatch.
  Latest runtime packet is TASK top; older paragraphs below are historical.
  PR262 merged c6034eb633f1ae09d005442f873b48e224cc4d87; CI34915983993 success16/16.
  Real pinned altool probe passed: upload-app/package, API key/issuer, p8 path and
  output-format supported. Fixed native receipt reader verified prior signing.
  Owner ASC setup STORED_METADATA_CONFIRMED, failure/cleanup null, write CONFIRMED;
  fresh metadata-only preflight ALREADY_PRESENT, protections pass. Do not repeat setup.
  Main implemented opt-in altool upload under IOS-TF-01/DEC111, frozen source review
  lease7 ACCEPT on codex/task-198-native-altool-upload. Direct79PASS1macOS SKIP,
  inspector18PASS; primary/secondary error and bare-macOS test corrections accepted.
  PR261 merged, CI34865517474 success16/16; runtime fingerprints match review.
  Owner stored three signing Secrets and five Variables; Main verified metadata,
  protection, exact staging ownership and no competing run, without payload access.
  Owner dispatched run34880271702 ONCE on main at base_commit, version1.0.0/build1.
  Run34880271702/job104097565748 SUCCESS: exact SIGNED_BASELINE_VERIFIED,
  signature/cleanup true, final audit ABSENT, artifact_count0. No duplicate rerun.
  Old run cancelled, one job record with no runner/steps; old transfer names absent.
  Native signing verified; upload/device not verified, staging Apple-login incomplete.
  Owner accepted persistent existing ASC upload key custody, recorded DEC111.
  One substantive PR/full hosted CI complete; no real upload yet. Nine accepted LF hashes in review.
  Reuse saved signing Secrets; no private input, key read or upload by Main.
  Existing four record edits travel with this substantive delivery, not a standalone PR.
worktree: C:/Users/USER/Repos/NTUBTOB-management-system
task: docs/coordination/tasks/TASK-198.md
report: docs/coordination/reports/TASK-198.md
review: docs/coordination/reviews/TASK-198.md
