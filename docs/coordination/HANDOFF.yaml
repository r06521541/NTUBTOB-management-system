active_task: TASK-068
status: changes_requested
next_actor: codex

base_commit: f1df7e3
head_commit: a6b8b10

updated_by: work
updated_at: 2026-08-07T23:16:18+08:00

note: >
  Work review accepted the Web Portal guard behavior but found two blocking fail-open inventory gaps. Add a fixed
  portal_rls_forced_count metric gated at zero. Strengthen audit drift checks to the exact Phase B relationship contract:
  reject every action/request shape outside member_backfilled/task065-member, identity_linked/task065-identity and
  qualification_granted/task065-team-player, and verify actor/auth/target, before/after state and deterministic request ID
  relationships. Add negative PostgreSQL fixtures for FORCE RLS, a status_changed row with task065 prefix, malformed
  request IDs and wrong before/after state. Preserve all prior guard behavior and repository-only boundaries.
