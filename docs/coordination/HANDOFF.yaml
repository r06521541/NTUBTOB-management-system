active_task: TASK-068
status: ready_for_codex
next_actor: codex

base_commit: f1df7e3
head_commit: null

updated_by: work
updated_at: 2026-08-07T23:05:00+08:00

note: >
  TASK-068 is ready for the dedicated Codex implementation session. Add a default-off, fail-closed Web Portal guard so
  match/ignore POST routes stop before DB or Discord side effects, keep the admin GET page read-only with a maintenance
  notice, and add a checksummed de-identified cross-model drift inventory/validator with local PostgreSQL tests. Do not
  implement dual-write, ignored mapping, Person activation, production access, schema/RLS changes, deployment or
  notification. Production Member/LINE mapping maintenance remains frozen.
