# TASK-196: rehearse Xcode archive/export without real credentials

Type delivery; delivery_group xcode-feasibility; L3. Owner approved TASK195's
Xcode-led architecture and bounded secret-free/fictional implementation on
2026-09-11. TASK195 design/security ACCEPT governs; actual INPUT_REJECTED cause
remains unknown. No real input, diagnostic retry, signing or upload authority.
Branch codex/task-196-xcode-feasibility; base/initial HEAD
5848831772017a039bbcccfa68e75ac849f89236. Seven carried Main-owned TASK194/195
records remain preserved and join this substantive delivery.
Main /root main-work task-196-main-20260911 lease1; TASK195 claims completed.

## Execution checkpoint

Goal: executable fictional evidence for no-key archive/manual export orchestration,
temporary-Keychain import/cleanup and safe failure classification, not real validity.
Core: new isolated rehearsal/native fixture/tests; Main CI and coordination only.
Invariant: no private paths/inputs, no automatic provisioning, upload or account;
existing CMS/native trust, intake and release readiness code remain unchanged.
Tests: tests-first boundary corpus, native macOS fixture and unsigned archive proof,
independent security acceptance followed by one hosted gate; Windows skips explicit.
Unknown: unsigned archive signing without project code after key import is unproven;
expected export rejection is not proof of positive export or real credential safety.

## Implementation scope

Build a bounded fictional-only entry with no arbitrary credential/path/live adapter.
Pin actual macOS/Xcode/build/SDK before native work; Main resolves supported hosted
image/toolchain. Inspect installed export help for exact manual export keys. Use
no-key archive preparation; a self-contained no-dependency fictional iOS app is
permitted to isolate Xcode feasibility, but is NOT a production Runner archive.
Native temporary-Keychain fixture must import only generated fictional PKCS12,
check key/certificate match and cleanup using reviewed native APIs; do not modify
default/login Keychain or global search list, pass passwords on argv or log contents.
Generate fixture material internally and isolate all files in validated task temp
paths. Existing memory-only helper is not this new persistent temporary fixture.
No project/package code after fictional key introduction: archive first, temporary
custody rehearsal separately, manual export only; no build/retry/auto-sign fallback.
Separate successful control/cleanup tests from expected no-Apple-profile export
rejection, unsupported toolchain and inconclusive outcomes. No blanket PASS implies
that Xcode can positively export the existing real profile or sign an unsigned app.
Bound subprocess time/output; suppress raw log/resultbundle/archive uploads; cleanup
on normal/failure/cancel where process survives, with hard-kill cleanup unproven.
Cover wrong target/key, unsafe path/symlink, output size/sentinel, failure/cancel,
cleanup failure, no provisioning/upload and absent secrets in workflow contract.
No new dependency unless Main reviews a genuine need. Existing source/readiness and
intake tools remain frozen. Future real custody/signing needs its own exact gate.

## Sole writer packet

task=TASK-196; branch/base/head above; actor_id=/root/csr_writer;
role=codex-writer; claim_id=task-196-writer-20260911; lease_version=1;
scope=new fictional Xcode/native custody rehearsal and focused tests;
owned_paths=tools/ios_xcode_feasibility.py,
tools/native/ios_temporary_keychain_rehearsal.swift,
tools/tests/test_ios_xcode_feasibility.py,
docs/coordination/reports/TASK-196.md;
write=allowed; report_to=/root;
stop_conditions=real assets, unsupported native authority, trust relaxation,
scope expansion or required new private access.
Use mandatory COLLABORATION2 ACK/heartbeat/immediate blocker/proactive completion
packet verbatim. Self-review/self-test then read-only; no Git/API mutation. Main
concurrently owns workflow/toolchain research, integration and documentation.

## Review and integration

Main owns .github/workflows/flutter-tests.yml, .github/workflows/python-tests.yml,
tools/tests/test_ci_workflow_contract.py,
TASK194/195 carried records, TASK196 task/review, HANDOFF, PROJECT_STATE and the
architecture entry in DECISIONS. No separate planning/status PR. Independent
/root/task181_review advisor claim will activate after writer handoff; no costly
hosted/native work before review. Standing Git authority covers one delivery PR
and hosted gate, not Apple/Secret/provider/runtime operations.

Hosted pin from official actions/runner-images macos-15 inventory: Xcode26.3
build17C529, iPhoneOS26.2; dedicated fictional job selects DEVELOPER_DIR without
changing the existing product compile job. macos-15 is an OS-family alias, not an
immutable runner image. Rehearsal must verify observed versions before native work;
missing/mismatched tools stop, no install/fallback. Unit tests run Windows/Linux too.

## Independent final reviewer packet (pending activation)

task=TASK-196; branch/base/head above; actor_id=/root/task181_review;
role=advisor; claim_id=task-196-security-20260911; lease_version=1;
owned_paths=none; write=read-only; report_to=/root;
scope=entire new rehearsal/native/tests and Main workflow/docs, focusing on bounded
process/path/output handling, temporary-Keychain custody and cleanup, no-private
entry/CI isolation, no-auto/no-upload and honest evidence/readiness boundaries;
stop_conditions=private access, unsupported policy expansion or unsafe evidence.
Mandatory COLLABORATION2 packet applies; do not activate until writer completes
and Main switches HANDOFF. No Git/API/private mutation or hosted execution.

Writer lease1 completed/read-only; four new owned paths handed off, Main received
and handled completion. Advisor task-196-security-20260911 lease1 NOW ACTIVE
against this frozen dirty implementation at unchanged full HEAD. Main does not
edit code during review. Writer reports10 tests8PASS2platform skips; native pending.

Security lease1 ACCEPT; Main received/handled final packet, reviewer read-only.
Main48test/quality evidence and reviewer27test evidence recorded in review.
One ready delivery PR now permitted under standing Git authority; hosted native
and full required gate must succeed before merge. No real signing/private entry.

## Bounded fictional native recovery

Initial source67d4c348762b04e255f6e954b849dce9b9a4d9f3, PR249,
run34568143647 native job103164413215: unit10PASS; exact Xcode26.3/17C529,
SDK26.2/macOS15.7.9/24G830 arm64 verified; unsigned fixture archive succeeded.
Custody returned CLEANUP_UNRESOLVED; raw internal result was suppressed, so exact
cause remains unknown and export was not reached. No private asset used.
Activate writer task-196-writer-20260911 lease2, same four owned paths, new
HEAD67d4c348762b04e255f6e954b849dce9b9a4d9f3; reviewer lease1 completed.
Scope: one bounded fictional layer-split using fixed allowlisted native phase/result
categories and tests, followed by at most one source correction when evidenced.
No raw stdout/OS payload, weaker guards, private access, or old intake diagnostic.
Unknown native behavior stays unresolved. Further same blocker after correction
returns inconclusive; no repeated guessing. Review before each changed hosted slice.

Writer lease2 completed/read-only; fixed native six-field taxonomy and case index
implemented without relaxing native target/API/ACL/timeouts/predicates. Activate
advisor task-196-security-20260911 lease2 against this dirty delta at HEAD67d4c348762b04e255f6e954b849dce9b9a4d9f3;
same scope/owned_paths=none/report_to=/root. Main native diagnostics remain pending.

Security lease2 ACCEPT, received by Main; reviewer read-only. Initial full run
completed FAILURE only for new native job/aggregate; all other selected jobs PASS.
Main may push reviewed diagnostic delta to same PR249 for the one fictional split.

Diagnostic source29df9e3527628cd6555379c324d2ef916433cfb4 run34569286534
native job103167763448:12testsPASS/archivePASS, case0 PATH_REJECTED at temp_binding,
cleanup NOT_CREATED and controller cleanup_verified=true. No Keychain was created;
no import/export reached. The assumed Foundation-temp/current-directory equality
failed. No cause beyond that predicate is inferred from this result.
Activate writer task-196-writer-20260911 lease3, same four owned paths, HEAD above.
Scope: one evidenced correction to native temporary-path binding using documented
canonical temp-root/cwd provenance, tests first. Preserve strict symlink/owner/mode/
root confinement and no arbitrary paths; no guess-driven relaxation or fallback.
Review public API semantics rather than another hosted diagnostic. If same native
blocker persists after this correction, stop inconclusive without further iterations.

Writer lease3 completed/read-only. Activate advisor task-196-security-20260911
lease3 at HEAD29df9e3527628cd6555379c324d2ef916433cfb4 for this sole OS-temp
binding correction; same read-only scope/packet/report_to=/root. Tests15run13PASS
2platform skips; native success not inferred. Main received writer completion.

Security lease3 ACCEPT received/handled by Main; reviewer read-only. Main now
integrates this one source correction into existing PR249; no further diagnostic
or speculative correction loop. Any remaining native blocker is reported honestly.
