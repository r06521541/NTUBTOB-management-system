# TASK-200: Evidence from the built iOS privacy manifests

Type: delivery; delivery_group: ios-privacy-inventory-20260916; risk: L3 CI boundary.
Base: a8b10c3e9804dc89eb0459917580c02405d3dec3.
Branch: codex/task-200-ios-privacy-inventory.
Main: /root; claim_id: task-200-main-20260916; lease_version: 1.

Status: completed; explicit Owner approval received2026-09-17 for exact
public15-file payload139e4cda32b3203050b7f3891fbb00785694b518. Guarded push
succeeded and sole ready PR268 completed16PASS, merged as
974ff5ad195057e49c3facd776948990a9c391c2; main/run35127733708 also16PASS.
Accepted source commit:0832b7110b390dac444b759e6b04b1c631703e02.
Both unsigned inventory summaries match; actual facts/findings are in report.
Main task/advisor claims released. No runtime rollout or public-release claim.
Six local closeout/evidence documents await the next substantive PR, not a
direct-main commit or status-only PR.

## Outcome and authority

Owner requested the next useful work and immediate continuation. Main selects
the missing native SDK/privacy evidence from the existing unsigned fictional
iOS CI build. This source/isolated-test package does not freeze Flutter UX or
require a policy/retention decision. Existing scoped repository commit/push/PR/
CI/merge authority applies after independent acceptance; destination remains
the same public repository. Preserve five Main-owned TASK199 closeout docs.

No signing, upload, store answers, real credentials/artifacts, deployment,
migration, provider/Secret mutation, private data, new dependency or paid service.
Do not create a new release controller or weaken any existing release gate.
Use the already-built unsigned application and resolved dependency metadata,
not an extra candidate/build/signing run. The USD20 aggregate cap is not reset.

Verification budget: focused local inventory/inspector/release/CI suites, then
the unchanged classifier's normal hosted full gate because a workflow changes;
no unrelated local Flutter/database rerun. Main maintains this evidence step.
It prevents source-file absence from being mistaken for packaged SDK absence;
the existing archive gate proves compilation but emits no privacy inventory.
This is not a substitute for Apple's privacy report/validator. If superseded,
replace/remove this isolated step only through review while preserving findings
and candidate gates; never convert STOP into success or loosen signing checks.

## Five-line execution checkpoint

1. Outcome: bounded offline inventory of packaged privacy manifests and native
   dependency metadata, with clear found/missing/unknown classifications.
2. Core paths: tools/ios_privacy_inventory.py and its direct tests; existing
   flutter-tests.yml unsigned archive job and CI contract tests; release docs.
3. Invariants: inventory is neither legal compliance nor proof of SDK runtime
   behavior/required-reason correctness; missing evidence is not a clean bill.
   No raw private paths, identifiers, tracking domains, plist dumps or exceptions.
4. Evidence: independent design challenge before implementation; synthetic
   real-format plist/package fixtures, malformed/oversized/linked/absent inputs,
   deterministic sanitized output, direct suite and existing hosted archive.
5. Stops: actual private material, new publishing/custody/cost, unknown policy or
   need to change signing/release authority. Record actionable safe stage/check
   and preserve discovered limitations; do not ask Owner to supply secrets.

## Design review packet

task=TASK-200; branch=codex/task-200-ios-privacy-inventory;
base=head=a8b10c3e9804dc89eb0459917580c02405d3dec3;
actor_id=/root/privacy_foundation; role=advisor;
claim_id=task-200-privacy-review; lease_version=1; report_to=/root;
scope=challenge bounded unsigned-artifact inventory design and necessary
 evidence semantics, then later independent source acceptance on new lease;
owned_paths=none; write=read-only;
stop_conditions=private assets/data, external mutation, authority expansion.

Apply COLLABORATION section2 mandatory protocol: immediate received/executing
with exact actor/claim/lease/report_to; heartbeat10–15min, immediate blocker,
proactive completion with fullSHA/dirty paths/tests/findings/limits/mutations.
Main implements while advisor inspects existing inventory/CI/test boundaries;
implementation starts after design acceptance. Reviewer never accepts own work.

## Delivery

Design ACCEPT received from /root/privacy_foundation lease1. Main accepts:
typed-shape inventory only, not an Apple enum/reason/runtime validator; absent
keys remain undeclared, not false/empty. Bounded path/read failures are STOP
with fixed stage/check; complete scans with missing/malformed declarations are
INVENTORY_COMPLETE_WITH_FINDINGS. Inspect both known SPM lock slots, preserve
conflicts/unknown schemas, no raw URL/revision/identity output. Exact public SDK
aliases only; lock resolution is not linkage. Source/run association plus
inspected-evidence digest are not whole-archive or signed-candidate attestation.
Signed IPA inspector and vendor/application manifests remain unchanged.

Source review: same actor/claim, lease_version=2, advisor/read-only, base/head
a8b10c3e9804dc89eb0459917580c02405d3dec3; exact ACK received. Frozen dirty paths:
tools/ios_privacy_inventory.py, tools/tests/test_ios_privacy_inventory.py,
tools/tests/test_ci_workflow_contract.py, .github/workflows/flutter-tests.yml.
Review also covers iOS README, privacy draft/checklist and task/report/review;
Main may append evidence records while source is frozen. Same report_to and
stop/heartbeat/proactive completion protocol apply. No external writes by advisor.
Lease2 completed with proactive ACCEPT, independent72PASS/2WindowsSKIP,
quality/diffcheck PASS and four LF fingerprints in review. Main accepts the
bounded source for final commit/normal PR; packaged facts were CI-pending at
that source checkpoint. PR/main evidence now closes that bounded requirement,
not the separate signed-candidate/runtime/privacy compliance gates.

One substantive PR including prior closeout records; no status-only PR.
Use existing classifier and standard hosted unsigned build, no paid/new runner.
Capture source/artifact-binding limits and actual manifest/dependency facts in
the existing privacy draft, not invented store answers. A defect in an actual
SDK manifest is a finding for separate scoped correction, not reason to silently
edit vendor files or add fabricated application declarations to get green.
