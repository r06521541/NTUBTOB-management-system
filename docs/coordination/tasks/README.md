# Active tasks

本目錄只放當前 delivery group 尚未封存的 `TASK-xxx.md`。沒有 active task 時只保留本說明。

建立 task 時必須標記 `task_type`、`delivery_group` 與 `requires_independent_pr`；完成一個 Phase 或約十個 tasks
後由 Work 依 `COLLABORATION.md` 封存。
